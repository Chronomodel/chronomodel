#ifndef MATRIX_H
#define MATRIX_H

#include <vector>
#include <cmath>
/*
class Matrix
{
public:
    Matrix();
   // Matrix(std::size_t N);
    //double& at(std::size_t i, std::size_t j);
    //double& operator[](std::size_t i, std::size_t j);

    virtual ~Matrix();
private:
    std::size_t N;
    std::vector<double> data;
};
*/
#endif // MATRIX_H
